#include "utils.hpp"
#include <stack>
#include <iostream>

  
int Calculate(const std::string& data) {
vector <string> data
    

    switch (operation) {
    case '-':
        std::cout << "a - b = " << a - b << '\n'<<std::endl;
        break;
    case '+':
        std::cout << "a + b = " << a + b << '\n'<<std::endl;
        break;
    case '*':
        std::cout << "a * b = " << a * b << '\n'<<std::endl;
        break;
            
    default:
        std::cout << "Error\n";
    }
    return 0;
}


 